using System;

namespace exer8
{
    class Notas
    {
        public double Media(double nota1, double nota2, double nota3)
        {
            return ((nota1*1) + (nota2*2) + (nota3*3))/6;
        }
    }
}
